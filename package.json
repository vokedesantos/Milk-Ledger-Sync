{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,

  "scripts": {
    "build": "pnpm -r --if-present run build",
    "typecheck": "tsc -b",
    "start": "node dist/index.js"
  },

  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
